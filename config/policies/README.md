# Managed Policies

Sample policy files live under `config/policies/managed/`. Copy them into the
managed-policy location for the Chromium build you are deploying, then adjust
the extension IDs and policy values for that environment.

These samples document intent. Chromium policy availability can change by
version and distribution, so verify against the policy list for the exact build.
